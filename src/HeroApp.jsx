import React from 'react';
import 'animate.css';
import { AppRouters } from './components/routers/AppRouters';

const HeroApp = () => {
    return (
      <AppRouters/>
    );
};

export default HeroApp;
